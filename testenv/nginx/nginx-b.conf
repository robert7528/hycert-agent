server {
    listen 443 ssl;
    server_name test-b.local _;

    ssl_certificate     /etc/nginx/ssl/cert.pem;
    ssl_certificate_key /etc/nginx/ssl/key.pem;

    location / {
        default_type text/plain;
        return 200 "nginx-b (cert-b) OK\n";
    }
}
